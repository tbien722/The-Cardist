---
import "../styles/global.css";
const base = import.meta.env.BASE_URL;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>The Cardist</title>
  </head>
  <body>
    <main>
      <h1 class="site-title">The Cardist</h1>
      <section class="landing-grid">
        <a class="landing-card" href={`${base}decks/`} style={`background-image: url('${base}cards/decks-homepage.webp')`}>
          <span class="landing-label">Decks</span>
        </a>
        <a class="landing-card" href={`${base}cardistry/`} style={`background-image: url('${base}cards/cardistry-homepage.webp')`}>
          <span class="landing-label">Cardistry</span>
        </a>
      </section>
    </main>
  </body>
</html>

